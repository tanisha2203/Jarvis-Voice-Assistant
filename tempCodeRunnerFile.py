def aiProcess(command):
#     # Initialize the Cohere client with your API key
#     co = cohere.Client('xyz')

#     # Generate a response using the Cohere text generation model
#     response = co.generate(
#         model='command-xlarge-nightly',  # You can adjust the model as needed
#         prompt=f"You are a virtual assistant named Jarvis skilled in general tasks like Alexa and Google Cloud. Give short responses please.\nUser: {command}\nJarvis:",
#         max_tokens=50  # Limit the number of tokens for shorter responses
#     )

#     # Return the generated text
#     return response.generations[0].text.strip()
